#Se o ano é bissexto ou não
#ano bissexto e divisível por 4, mas não por 100, se for por 100 , tem que ser divisivel por 400 
ano = int(input("digite o ano que deseja checar:"))

if ano % 4 == 0:
    if ano % 100 == 0:
        if ano % 400 == 0:
            print("Esse ano é bissexto")
        else:
            print(" Ele não é bissexto")
    else:
        print("Ele é bissexto")
    
else:
    print("Esse ano não e bissexto")
